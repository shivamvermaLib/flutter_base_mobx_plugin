name: flutter_base_mobx_plugin
description: A new flutter plugin project.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
    
  mobx: ^1.2.1
  flutter_mobx: ^1.1.0
  page_transition: ^1.1.5
  get_it: ^4.0.2
  firebase_auth: ^0.16.1
  cloud_firestore: ^0.13.6
  dart_json_mapper:
  dart_json_mapper_mobx:


dev_dependencies:
  flutter_test:
    sdk: flutter
  mobx_codegen: ^1.0.3
  build_runner: